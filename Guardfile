# encoding: utf-8

load 'nanoc/setup.rb'

guard('nanoc') do
  watch('README.md')
  watch('Rules')
  watch(/^nanoc/)
  watch(/^nanoc\//)
  watch(/^features\//)
  watch(/^lib\//)
  watch(/^spec\//)
end
