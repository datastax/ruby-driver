<h2>Direct Known Subclasses</h2>
<div class="subclasses">
  <ul class="list-inline">
    <% @subclasses.each do |name, child| %>
    <li><code><%= linkify(child, name) %></code></li>
    <% end %>
  </ul>
</div>
