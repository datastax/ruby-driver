<% @inner.each do |name, list| %>
<% next if list.empty? %>
<h2><%= name.to_s.capitalize %></h2>
<div class="child-<%= name %>">
  <ul class="list-inline">
    <% list.each do |child| %>
    <li><code><%= linkify(child, child.name(true)) %></code></li>
    <% end %>
  </ul>
</div>
<% end %>
